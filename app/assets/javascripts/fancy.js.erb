$(function() {
  $('#task_target_date').datepicker({ dateFormat: 'yy-mm-dd' });
});

$(function() {
		$( "#task_actual_date" ).datepicker({ dateFormat: 'yy-mm-dd' });
	});

$(function() {
	  $('#lead_target_date').datepicker({ dateFormat: 'yy-mm-dd' });
	});
	
$(function() {
	  $('#contact_activity_activity_date').datepicker({ dateFormat: 'yy-mm-dd' });
	});